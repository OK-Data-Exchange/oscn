<!DOCTYPE html>
<html>
<head>
  <title>Tulsa PD</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

  <!-- Font Awesome -->
  <link
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
    rel="stylesheet"
    />
  <!-- Google Fonts -->
  <link
    href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap"
    rel="stylesheet"
    />
  <!-- MDB -->
  <link
    href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/7.1.0/mdb.min.css"
    rel="stylesheet"
    />
</head>

<body class="bg-opacity-10 bg-secondary">

<div class="container">
  <%# if current_user.present? %>
<!--    <p class="text-end mt-2 mb-0">-->
<!--      <%#= link_to destroy_user_session_path, method: :delete do %>Log out<%# end %>-->
<!--    </p>-->
    <%# if current_user.admin_role? %>
<!--      <p>Visible only for superadmins! </p>-->

<!--      <%#= link_to "Create User", '/users/sign_up' %><br/>-->

<!--      <ul>-->
        <%# @users.where(admin_role: false).each do |user| %>
<!--          <li>-->
<!--            <div><%#= user.email %></div>-->
            <%#= link_to 'Edit User', user_path(user) %>
<!--            <div>Unhappy? <%#= button_to "Cancel my account", user_delete_path(user), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete %></div>-->
<!--          </li>-->
        <%# end %>
<!--      </ul>-->
    <%# end %>
  <%# end %>
  <div class="row">
    <div class="col-sm-2">
      <h1><i class="fa-solid fa-scale-balanced" style="font-size: 200px;margin-top: 20px;color: #b9bfca;"></i></h1>
    </div>
    <div class="col-sm-10">
      <% flash.each do |type, msg| %>
        <div class="alert alert-<%= type == 'notice' ? 'info' : 'warning' %> mt-3 mb-1">
          <%= msg %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </div>
</div>

<script
  type="text/javascript"
  src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/7.1.0/mdb.umd.min.js"
></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
<script type="application/javascript">
  const copyLink = document.querySelectorAll('[data-copy]')
  copyLink.forEach((el) => {
    el.addEventListener('click', (_e) => {
      e.preventDefault
      const text = el.innerHTML.trim();
      copy(text);
    })
  })

  const copy = (text) => {
    if (navigator.clipboard) {
      navigator.clipboard.writeText(text)
    } else {
      const input = document.createElement('textarea')
      input.value = text
      document.body.appendChild(input)
      input.select()
      document.execCommand('copy')
      document.body.removeChild(input)
    }
  }
</script>
</body>
</html>
